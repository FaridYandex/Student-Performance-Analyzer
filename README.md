# 🎓 Student Performance Analyzer & Recommender System

[![Tests](https://github.com/YOUR_USERNAME/student-performance-analyzer/actions/workflows/ci_tests.yml/badge.svg)](https://github.com/YOUR_USERNAME/student-performance-analyzer/actions/workflows/ci_tests.yml)
[![Daily Report](https://github.com/YOUR_USERNAME/student-performance-analyzer/actions/workflows/daily_report.yml/badge.svg)](https://github.com/YOUR_USERNAME/student-performance-analyzer/actions/workflows/daily_report.yml)

## 📌 Описание проекта
**Student Performance Analyzer** — это интеллектуальная система анализа успеваемости студентов с функцией генерации персонализированных рекомендаций учебных материалов. Проект анализирует данные об оценках и посещаемости, выявляет проблемные предметы и предлагает студентам индивидуальные рекомендации для улучшения результатов.

**Ключевые возможности:**
- Комплексный анализ успеваемости по предметам
- Визуализация прогресса с графиками и диаграммами
- Генерация персонализированных рекомендаций на основе слабых мест
- Автоматическая ежедневная генерация отчетов (публикуется на GitHub Pages)
- Поддержка русского и английского языков
- Контейнеризация с помощью Docker

## 🚀 Установка и использование

### 1. Клонирование репозитория
```bash
git clone https://github.com/YOUR_USERNAME/student-performance-analyzer.git
cd student-performance-analyzer
```

### 2. Установка зависимостей
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate  # Windows

pip install -r requirements.txt
```

### 3. Запуск анализа (локально)
```python
from src.data_loader import load_data
from src.analyzer import analyze_student_performance
from src.recommender import generate_recommendations

# Загрузка данных
df = load_data("data/sample_grades.csv")

# Анализ успеваемости для студента ID=1
performance = analyze_student_performance(df, student_id=1)
print(f"Средний балл студента: {performance['average_grade']}")
print(f"Слабые предметы: {', '.join(performance['weak_subjects'])}")
print(f"Уровень риска: {performance['risk_level'].upper()}")

# Генерация рекомендаций
recommendations = generate_recommendations(performance['weak_subjects'], performance['average_grade'])
print("\nРекомендуемые материалы:")
for rec in recommendations:
    print(f"- {rec['title']} ({rec['difficulty']}): {rec['url']}")
```
### Ожидаемый результат:
Средний балл студента: 82.33
Слабые предметы: Physics
Уровень риска: LOW

Рекомендуемые материалы:
- Newton's Laws (beginner): https://example.com/physics1
- Quantum Mechanics Intro (advanced): https://example.com/physics2

### Генерация HTML-отчета
```python
from src.report_generator import generate_student_report

# Создание отчета для студента ID=1
generate_student_report(df, student_id=1, output_path="docs/report_student_1.html")
print("Отчет успешно сохранен: docs/report_student_1.html")
```

## 🐳 Запуск через Docker

### 1. Сборка Docker-образа
```bash
docker build -t student-analyzer .
```

### 2. Запуск контейнера
```bash
# Базовый запуск (английский язык по умолчанию)
docker run --rm -v $(pwd)/docs:/app/docs student-analyzer

# Запуск с русским языком
docker run --rm -e APP_LANGUAGE=ru -v $(pwd)/docs:/app/docs student-analyzer

# Запуск с конкретным студентом (если скрипт поддерживает аргументы)
docker run --rm -e STUDENT_ID=1 -v $(pwd)/data:/app/data -v $(pwd)/docs:/app/docs student-analyzer
```

### 3. Параметры команды
- --rm — автоматическое удаление контейнера после завершения
- -e APP_LANGUAGE=ru — установка переменной окружения для выбора языка
- -v $(pwd)/docs:/app/docs — монтирование локальной папки docs в контейнер для сохранения отчетов
- -v $(pwd)/data:/app/data — монтирование папки с данными (опционально, если нужно использовать свои данные)

### 4. Проверка результатов
После выполнения команды проверьте папку docs в вашем проекте:
```bash
ls -la docs/
# Вы увидите сгенерированные HTML-отчеты и графики
```

## 🌐 Переключение языка
```python
from src.utils.i18n import set_language, get_text

# Установка русского языка
set_language("ru")
print(get_text("welcome_message"))
# Вывод: "Добро пожаловать в систему анализа успеваемости!"

# Установка английского языка
set_language("en")
print(get_text("welcome_message"))
# Вывод: "Welcome to the Student Performance Analyzer!"
```

## 📂 Структура проекта
├── .github/workflows       # Конфигурации GitHub Actions
│   ├── ci_tests.yml        # Автоматические тесты и линтинг
│   ├── daily_report.yml    # Ежедневная генерация отчетов
│   └── docker_build.yml    # Сборка Docker-образа
├── data                    # Данные для анализа
│   ├── sample_grades.csv   # Пример данных об успеваемости
│   └── curriculum.json     # База учебных материалов
├── docs                    # Сгенерированные отчеты (публикуется на GitHub Pages)
│   └── report_student_1.html
├── locales                 # Файлы локализации
│   ├── en.json             # Английские переводы
│   └── ru.json             # Русские переводы
├── scripts                 # Служебные скрипты
│   └── generate_daily_report.py  # Основной скрипт генерации отчета
├── src                     # Основной код приложения
│   ├── analyzer.py         # Модуль анализа успеваемости
│   ├── data_loader.py      # Загрузка и валидация данных
│   ├── recommender.py      # Система рекомендаций
│   ├── report_generator.py # Генерация HTML-отчетов
│   └── utils               # Вспомогательные функции
│       ├── __init__.py
│       ├── i18n.py         # Мультиязычная поддержка
├── tests                   # Unit-тесты
│   ├── test_analyzer.py
│   ├── test_i18n.py
├── .gitignore              # Файлы для исключения из Git
├── Dockerfile              # Конфигурация Docker-контейнера
├── requirements.txt        # Основные зависимости
├── requirements-dev.txt    # Зависимости для разработки
└── README.md               # Эта документация

